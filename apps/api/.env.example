# --- Server ---
PORT=4000
WEB_ORIGIN="http://localhost:3000"

# --- Database (Postgres) ---
DATABASE_URL="postgres://aidar:aidar@localhost:5432/aidar_waitlist"

# --- Email (Resend) ---
# Leave RESEND_API_KEY empty in dev to skip sending (submissions still succeed, emails are logged).
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Aidar <hello@notifications.getaidar.com>"
WAITLIST_ADMIN_EMAIL="team@getaidar.com"

auto-install-peers=true
strict-peer-dependencies=false
# Hoisted node_modules (npm-like single tree). Keeps Docker multi-stage copies
# simple and predictable for Coolify, at the cost of pnpm's strict phantom-dep
# isolation. See COOLIFY-DEPLOYMENT.md.
node-linker=hoisted
